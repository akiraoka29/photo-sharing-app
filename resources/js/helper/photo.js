jQuery(function($) {
    // Initialize
    let Props = {
        element: {}
    }
    let Photo = {}

    // Call Function
    Photo.init = () => {
    }

    Photo.active = {
    }

    Photo.event = {
    }

    // Render
    Photo.init();
})